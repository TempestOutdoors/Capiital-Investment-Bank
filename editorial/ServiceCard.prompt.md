A service on the deep-sea ground.

```jsx
<ServiceCard icon="gauge" title="Performance Visibility"
  body="KPI architecture, management reporting and the dashboards your investors actually read."
  quote="We finally agreed what to count — and then the arguments stopped." />
```

- Two across in a 1px grid over `--rule-on-ink`. Four services is the set the site ships with.
- Use `icon` on the services page and `number` in denser listings; never both.
- `quote` is one sentence in a client's words. Leave it off rather than inventing one.
