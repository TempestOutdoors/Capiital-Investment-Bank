One-line: text field for forms (contact, client portal); 36px tall, transparent ground, taupe focus border.

```jsx
<Input placeholder="Institutional email" />
```

Pair with `Label`. Placeholder colour is `--muted-foreground`. Disabled renders at 50% opacity.
