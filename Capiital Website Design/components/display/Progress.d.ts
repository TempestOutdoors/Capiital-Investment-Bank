import * as React from "react";
/** 8px determinate bar, bone track, ink fill. */
export interface ProgressProps { value?: number; style?: React.CSSProperties }
export function Progress(props: ProgressProps): React.ReactElement;
