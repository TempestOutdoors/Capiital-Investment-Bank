One-line: labels a status or category (sector tag, deal stage).

```jsx
<Badge variant="secondary">Industrials</Badge>
```

For editorial metadata prefer the `.eyebrow` class over a Badge — the site uses caps-and-tracking, not chips.
