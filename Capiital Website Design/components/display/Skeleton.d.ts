import * as React from "react";
/** Loading placeholder block in --muted. No shimmer animation on this brand. */
export interface SkeletonProps { style?: React.CSSProperties }
export function Skeleton(props: SkeletonProps): React.ReactElement;
