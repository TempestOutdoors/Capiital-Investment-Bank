One-line: the small tracked caps line above every section heading.

```jsx
<Eyebrow>§ 03 — Selected Transactions</Eyebrow>
```

Sections are numbered with a section sign and an em dash: `§ 01 — The Firm`. On the navy ground pass `style={{color:"var(--taupe-soft)"}}` or wrap in `.on-ink`.
