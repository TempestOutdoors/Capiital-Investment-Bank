One-line: a single proof figure (`$420B+`, `1994`, `62`).

```jsx
<StatBlock value="$420B+" label="Aggregate transaction value" tone="cream" />
```

Four across on the hero, three across in the firm section, separated by a top hairline.
